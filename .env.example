# Optional. Empty = fully functional local mock AI.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional absolute path; default: backend/data/taskquest.db
# DATABASE_PATH=

# Docker host port; container listens on 8000
APP_PORT=8080
